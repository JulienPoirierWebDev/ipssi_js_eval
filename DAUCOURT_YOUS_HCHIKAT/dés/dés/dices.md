    // ============================================
    		// 🎯 Objectif : logique de jeu multi-tours avec stratégie
    		// ============================================


            Vous devez créer un jeu de dès. Il y a deux joueurs. Lorsque c'est le tour du joueur 1, il lance le dès autant de fois qu'il le souhaite, tant qu'il ne fait pas 1. S'il tombe sur un, il perd son tour. Il doit donc choisir lui-même d'arreter son tour avant de tomber sur 1.

            Lorsqu'il décide d'arrêter son tour, le score de son tour est ajouté à son score total. Le premier joueur à atteindre 30 points remporte la partie.



    		// Étape 1️⃣ : récupérer tous les éléments


    		// Étape 2️⃣ : définir l’état du jeu
    		// Il faut savoir quel joueur est actif, ses scores, etc.

    		// Étape 3️⃣ : fonction pour lancer un dé (retourne un nombre et emoji du tableau faces)

    		// Étape 4️⃣ : logique du tour

    		// Lorsque le joueur clique sur "Lancer le dé", le dé est lancé.
    		// A vous de définir la logique.

    		// Pensez a mettre à jour l'affichage après chaque action.

    		// Étape 5️⃣ : garder le score du tour et passer au suivant
    		// Lorsque le joueur clique sur "Garder le score", le score du tour est ajouté au score total.
    		// Puis on passe au joueur suivant.
    		// Il faut vérifier si le jeu continu et mettre à jour l'affichage.
